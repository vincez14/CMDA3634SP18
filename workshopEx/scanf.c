#include <stdio.h>
#include <stdlib.h>

int num; // declare num as a variable of type int

void main(){

	scanf("%d", &num); // take an input and store (using &) in the variable, num

	printf("Your number is %d\n", num);
}

