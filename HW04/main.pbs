#! /bin/bash
#
#PBS -l walltime=00:10:00
#PBS -l nodes=1:ppn=16
#PBS -W group_list=newriver
#PBS -q open_q
#PBS -j oe

#cd $PBS_O_WORKDIR

#module purge

make

int Nthreads [7] = {1, 2, 4, 8, 12, 16, 20};

for (int i = 0; i < sizeof(Nthreads)/sizeof(struct Nthreads); i++) {
    ./main i
}
